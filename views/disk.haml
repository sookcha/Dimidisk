!!! 5
%html
	%head
		%title= "Dimidisk"
		= partial "meta/_meta"
	%body
		.menu
			%a.menu{:href=>'/'}
				%h1.logo Dimidisk
			%a.menu{:href=>'/logout'}
				%span.option{:class=>"radius button secondary"} Logout
			
		.container
			- @names.each_with_index do |name,i|
				- if i != 0
					.span2
						%a{:href => "shared/" +@ids[i].text}
							%p= name