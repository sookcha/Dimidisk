!!! 5
%html
	%head{:xmlns=>"http://www.w3.org/1999/xhtml",:"xmlns:og"=>"http://ogp.me/ns#" ,:"xmlns:fb"=>"http://www.facebook.com/2008/fbml"}
		- if params[:name] != nil
			%title= "Dimidisk - " + params[:name]
		- else
			%title= "Dimidisk"
		
		= partial "meta/__meta"
	%body
		.menu
			%a.menu{:href=>'/shared'}
				%h1.logo Dimidisk
			%a.menu{:href=>'/logout'}
				%span.option{:class=>"radius button secondary"} 로그아웃
		.container
			%h1.center 에러발생.
			- if params[:code] == "-1000"
				%span.center 다운로드 권한이 없습니다.
				%span.cetner= "에러코드" + params[:code]
				%a{:href => "#", :onClick=>"history.back();return false;"} 
					%p.center 뒤로가기